#include "main.h"

/**
 * _islower - checks whether character c is a
 * lowercase character.
 *
 * @c : character.
 *
 * Return: Either 1 or 0.
 */
int _islower(int c)
{
	return ((c > 96 && c < 123));
}
