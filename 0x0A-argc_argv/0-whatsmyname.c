#include <stdio.h>

/**
 * main - check code.
 * @argc : int - argument count.
 * @argv : char * - argument vector.
 * Return: Always 0.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
